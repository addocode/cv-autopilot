import {writeFileSync}from'node:fs';export interface RenderReport{success:boolean;variant:string;renderer:string;renderedAt:string;durationMs:number;pageCount:number;warnings:string[];overflows:{elementId:string;scrollHeight:number;clientHeight:number;overflowPixels:number;suggestion?:string}[]}export function writeReport(report:RenderReport){writeFileSync(`dist/render-report-${report.variant}.json`,JSON.stringify(report,null,2));return report}
