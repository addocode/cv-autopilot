import type{CvData,Variant}from'../types/cv.js';
export function mergeVariant(data:CvData,variant:Variant):CvData & {headline:string;summaryText:string}{const selected=new Set(variant.selectedBulletIds);const hidden=new Set(variant.hiddenBulletIds);const prio=new Map(variant.bulletPriority.map((id,i)=>[id,i]));const skillSections=variant.skillSectionOrder.map(id=>data.skillSections.find(s=>s.id===id)).filter((s):s is CvData['skillSections'][number]=>Boolean(s)).filter(s=>!variant.hiddenSkillSections.includes(s.id)).map(s=>({...s,title:variant.skillSectionTitles[s.id]||s.title}));const experiences=data.experiences.map(e=>({...e,bullets:e.bullets.filter(b=>selected.has(b.id)&&!hidden.has(b.id)).sort((a,b)=>(prio.get(a.id)??99)-(prio.get(b.id)??99)).slice(0,variant.maxBulletsPerExperience)}));const toolMap=new Map(data.tools.map(t=>[t.id,t]));const tools=variant.toolOrder.map(id=>toolMap.get(id)).filter((t):t is CvData['tools'][number]=>Boolean(t)).slice(0,variant.maxTools);return{...data,headline:variant.headline,summaryText:variant.summaryKey==='default'?data.summary.default:data.summary.variants[variant.summaryKey],skillSections,experiences,tools}}
