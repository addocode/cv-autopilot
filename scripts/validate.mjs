import {readFileSync}from'node:fs';const d=JSON.parse(readFileSync('data/private/cv.master.json','utf8'));const errs=[];if(!d.person?.name)errs.push('person.name missing');if(!d.experiences?.length)errs.push('experiences missing');for(const e of d.experiences)if(e.bullets.length>4)errs.push(`${e.id} has too many bullets`);if(errs.length){console.error(errs.join('\n'));process.exit(1)}console.log('CV data valid');
